name: actions
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
jobs:
  tests:
    runs-on: ubuntu-latest
  steps:
    uses: actions/checkout@v3
    uses: actions/setup-node@v1
    with:
      node-version: 12
    run: npm run test
